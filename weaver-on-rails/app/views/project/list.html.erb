<%= stylesheet_link_tag "list_page" %>
<%= render :partial => "elements/header", :locals => { :title => 'Projects' } %>
        <div id='container'>
<%= render :partial => "elements/nav_bar" %>
        	<div id='inner_container'>
              <div id="list_title">projects</div>
              <div id="list_container">
                <% @projects.each do |p| %>
                  <div class="list_item">
                    <div class="list_item_title"><%= link_to p.title, 'projects/' + p.title.downcase.gsub(' ', '_') %></div>
                    <div class="list_item_datetime"><%= p.created_at.strftime("%a, %b %d, %Y") %></div>
                    <div class="list_item_quote">“</div>
                    <div class="list_item_content"><%= p.description %></div>
                  </div>
                <% end %>
              </div>
        	</div>
        </div>
